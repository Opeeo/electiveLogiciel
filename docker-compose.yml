version: '3'
services:
  sensors:
    build: ./sensors
    ports:
      - 3001:3000
  db:
    image: 'mongo'
    ports:
      - "27017:27017"

  db_user:
    image: mysql:latest
    environment:
      - MYSQL_ROOT_PASSWORD=mysqlpw
    ports:
      - 3306:3306