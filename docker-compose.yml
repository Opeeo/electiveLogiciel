version: '3'

services:
  db_user:
    image: mysql:latest
    environment:
      - MYSQL_ROOT_PASSWORD=mysqlpw
    ports:
      - 3306:3306
  
  users:
    build: './users'
    ports:
      - "3002:3000"
    depends_on: 
      - db_user
