version: '3'

services:
  db_user:
    image: mysql:latest
    environment:
      - MYSQL_ROOT_PASSWORD=mysqlpw
    ports:
      - 3306:3306
    volumes:       
      - ~/apps/mysql:/data/db 

  profile:
    build: './Profile'
    ports:
      - 3003:3000
    depends_on:
      - db_user

  
  restaurant:
    build: './Restaurant'
    ports:
      - 3005:3000
  
  nginx:
    image: nginx:latest
    ports:
      - 8080:8080
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro
    depends_on:
      - restaurant
      - profile